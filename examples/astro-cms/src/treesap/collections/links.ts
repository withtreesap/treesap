import type { Collection } from "@treesap/treesap";

export const links: Collection = {
  slug: "links",
  label: "Links",
  fields: [
    {
      name: "title",
      label: "Title",
      type: "string",
    },
  ],
};